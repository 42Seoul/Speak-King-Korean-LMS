# 👋 LMS 프로젝트 기여 가이드 (Contributing Guide)

이 문서는 프로젝트 개발에 참여하는 팀원을 위한 협업 규칙과 개발 가이드라인입니다.  
개발을 시작하기 전에 반드시 읽어주세요.

---

## 🚀 1. 시작하기 (Getting Started)

### 1-1. 프로젝트 설치
```bash
# 1. 저장소 클론 (Clone)
git clone [레포지토리 주소]

# 2. 패키지 설치 (npm 사용)
npm install

1-2. 환경 변수 설정

이 프로젝트는 Supabase 및 외부 API 연동을 위해 환경 변수가 필요합니다.
	1.	프로젝트 루트에 있는 .env.example 파일을 복사하여 .env.local 파일을 생성하세요.
	2.	.env.local 파일 내부의 값은 노션(Notion) 문서를 확인하거나 팀장에게 메신저로 요청해주세요.
	3.	주의: .env.local 파일은 깃에 절대 업로드하지 않습니다.

1-3. 실행

npm run dev

브라우저에서 http://localhost:3000으로 접속하여 확인합니다.

⸻

🌳 2. Git 협업 규칙 (Workflow)

우리는 Github Flow 방식을 따릅니다.
main 브랜치는 언제나 배포 가능한 무결점 상태여야 합니다.

2-1. 브랜치 전략

절대로 main 브랜치에 직접 커밋(Commit)하거나 푸시(Push)하지 않습니다.
	1.	작업 시작 전 최신화

git checkout main
git pull origin main

	2.	작업 브랜치 생성

	•	포맷: 타입/기능요약
	•	예시: feature/login-page, fix/audio-player, refactor/api-logic

git checkout -b feature/speaking-test

2-2. 커밋 메시지 규칙 (Convention)

제목만 봐도 내용을 알 수 있도록 작성합니다.
	•	포맷: [타입] 작업 내용 요약
	•	타입 목록
	•	[Feat] 새로운 기능 추가
	•	[Fix] 버그 수정
	•	[Design] CSS 및 UI 디자인 변경
	•	[Refactor] 기능 변경 없는 코드 구조 개선
	•	[Chore] 설정 파일 변경, 패키지 설치 등
	•	예시
	•	[Feat] 말하기 평가 기능 구현
	•	[Fix] 로그인 페이지 버튼 깨짐 수정

2-3. Pull Request (PR) & Merge
	1.	작업이 완료되면 Github에 Push 합니다.
	2.	Compare & pull request 버튼을 눌러 PR을 생성합니다.
	3.	PR 제목은 커밋 메시지와 동일하게 작성합니다.
	4.	리뷰 승인(Approve)을 받은 후 Squash and Merge로 병합합니다.

⸻

🛠 3. 기술 스택 및 개발 표준 (Tech Spec)

3-1. 핵심 기술 스택
	•	Framework: Next.js 14+ (App Router)
	•	Language: TypeScript (Strict Mode)
	•	Styling: Tailwind CSS + shadcn/ui
	•	State Management
	•	Client: Zustand
	•	Server: TanStack Query
	•	Database: Supabase (Auth, DB, Storage)

3-2. 폴더 구조 (Directory Structure)

기능 단위가 아닌 역할(Role) 단위로 구분합니다.

├── app/
│   ├── (student)/
│   ├── (teacher)/
├── components/
│   ├── ui/
│   ├── shared/
│   ├── classroom/
├── lib/
│   ├── supabase/
│   └── utils.ts
├── hooks/
└── types/

3-3. 코딩 컨벤션
	1.	컴포넌트 분리 기준
	•	코드가 200줄을 넘어가면 분리를 고려합니다.
	•	2번 이상 재사용되는 코드는 반드시 별도 컴포넌트로 분리합니다.
	2.	UI 라이브러리
	•	디자인을 직접 고민하기보다 shadcn/ui 컴포넌트 사용을 권장합니다.
	3.	데이터베이스 타입
	•	Supabase DB 스키마 변경 시 아래 명령어로 타입을 갱신합니다.

npx supabase gen types typescript --project-id "프로젝트ID" > types/database.types.ts


⸻

⚙️ 4. 개발 환경 설정 (VS Code)

팀원 간 코드 스타일 통일을 위해 Prettier와 Format On Save를 사용합니다.

프로젝트 루트에 .vscode/settings.json 파일이 없다면 아래 내용을 추가해주세요.

{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  }
}

VS Code의 Prettier 확장이 설치되어 있어야 합니다.
