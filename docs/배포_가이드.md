# 🚀 Speak-King-Korean LMS 배포 가이드

## 목차
1. [배포 아키텍처 개요](#배포-아키텍처-개요)
2. [사전 준비사항](#사전-준비사항)
3. [Supabase 설정](#0-supabase-설정-배포-전-필수-)
4. [Railway 배포 (Python 백엔드)](#1-railway-배포-python-백엔드---먼저-배포)
5. [Vercel 배포 (Next.js)](#2-vercel-배포-nextjs)
6. [도메인 연결](#3-도메인-연결-선택)
7. [로컬 개발 환경](#4-로컬-개발-환경-선택)
8. [배포 후 테스트](#5-배포-후-테스트)
9. [문제 해결](#6-문제-해결)
10. [체크리스트](#7-체크리스트)

---

## 배포 아키텍처 개요

```
사용자
  ↓
커스텀 도메인 (예: myapp.com)
  ↓
┌─────────────────────────────────────┐
│   Vercel (Next.js 프론트엔드)       │
│   - /                               │
│   - /dashboard                      │
│   - /classroom                      │
│   - /api/* (Next.js API Routes)     │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│   Railway (FastAPI 백엔드)          │
│   - POST /create-sprite             │
│   - Supabase Storage 연동           │
└─────────────────────────────────────┘
  ↓
┌─────────────────────────────────────┐
│   Supabase (데이터베이스)           │
│   - PostgreSQL                      │
│   - Storage (오디오/이미지)         │
│   - Auth (인증)                     │
└─────────────────────────────────────┘
```

---

## 사전 준비사항

### ✅ 필수 계정
- [ ] GitHub 계정
- [ ] Vercel 계정 (https://vercel.com)
- [ ] Railway 계정 (https://railway.app)
- [ ] Supabase 계정

### ✅ 필수 정보
- [ ] Supabase 프로젝트 URL
- [ ] Supabase Anon Key
- [ ] Supabase Service Role Key
- [ ] Google Gemini API Key
- [ ] Teacher Secret Code (교사 가입 코드)

---

## 0. Supabase 설정 (배포 전 필수!) ⚠️

### 0.1 Authentication URL 설정

**반드시 배포 전에 설정해야 합니다!**

1. **Supabase 대시보드 접속**
   - https://app.supabase.com
   - 프로젝트 선택

2. **Authentication 설정**
   - 좌측 메뉴: `Authentication` → `URL Configuration`

3. **Site URL 설정**
   ```
   Production: https://your-project.vercel.app
   ```
   - Vercel 배포 후 받은 URL 입력
   - 배포 전에는 임시로 `http://localhost:3000` 사용 가능

4. **Redirect URLs 추가** ⭐ 중요!

   다음 URL들을 모두 추가:
   ```
   http://localhost:3000/auth/callback
   https://your-project.vercel.app/auth/callback
   ```

   - `Add URL` 버튼으로 각각 추가
   - localhost는 로컬 개발용
   - Vercel URL은 프로덕션용

5. **저장**
   - `Save` 버튼 클릭

### 0.2 확인 방법

설정이 올바른지 확인:
- Site URL: Vercel 배포 URL
- Redirect URLs: localhost + Vercel URL 모두 포함

**⚠️ 주의**: Redirect URLs에 등록되지 않은 URL로 리다이렉트 시도 시 에러 발생!

---

## 1. Railway 배포 (Python 백엔드) - 먼저 배포!

**⚠️ 중요: Railway를 먼저 배포해서 URL을 받은 후, Vercel 배포 시 환경 변수에 포함시켜야 합니다.**

### 1.1 GitHub에 코드 푸시

```bash
git add .
git commit -m "feat: 배포 준비 완료"
git push origin main
```

### 1.2 Railway 배포 파일 확인

다음 파일들이 `zepimg_111/` 폴더에 이미 준비되어 있습니다:

**Procfile** (✅ 이미 존재):
```
web: uvicorn api_server:app --host 0.0.0.0 --port $PORT
```

**runtime.txt** (✅ 이미 존재):
```
python-3.11
```

### 1.3 Railway 프로젝트 생성

1. **Railway 대시보드**
   - https://railway.app/dashboard 방문
   - "New Project" 클릭

2. **GitHub 저장소 연결**
   - "Deploy from GitHub repo" 선택
   - `skklms` 저장소 선택

3. **Root Directory 설정**
   - 프로젝트 생성 후 "Settings" 탭 클릭
   - "Root Directory"를 `zepimg_111`로 변경
   - "Update" 클릭

4. **환경 변수 설정**

   "Variables" 탭에서 다음 변수 추가:

   | 키 | 값 | 설명 |
   |---|---|---|
   | `GOOGLE_API_KEY` | `AIza...` | Google Gemini API Key |
   | `SUPABASE_URL` | `https://your-project.supabase.co` | Supabase URL |
   | `SUPABASE_SERVICE_ROLE_KEY` | `eyJhbGc...` | Supabase Service Role Key |

5. **배포 시작**
   - Railway가 자동으로 빌드 시작
   - "Deployments" 탭에서 진행 상황 확인
   - 약 3-5분 소요

6. **Public URL 생성 및 저장** ⭐
   - "Settings" 탭 → "Networking" 섹션
   - "Generate Domain" 클릭
   - `your-service.railway.app` 형태의 URL 생성됨
   - **⚠️ 중요: 이 URL을 복사하여 메모해둡니다!**
   - 예: `https://skklms-backend-production.railway.app`

**✅ Railway 배포 완료! 이제 Vercel 배포로 넘어갑니다.**

---

## 2. Vercel 배포 (Next.js)

Railway URL을 받았으므로, 이제 Vercel에 모든 환경 변수를 포함하여 한 번에 배포합니다.

### 2.1 코드 변경사항 확인

**sprite-maker 환경 변수 자동 적용** (✅ 이미 적용됨):
```typescript
// app/sprite-maker/page.tsx
const API_BASE_URL = process.env.NEXT_PUBLIC_SPRITE_API_URL || 'http://localhost:8000'
```

### 2.2 Vercel 프로젝트 생성

1. **Vercel 대시보드**
   - https://vercel.com/dashboard 방문
   - "Add New..." → "Project" 클릭

2. **GitHub 저장소 연결**
   - `skklms` 저장소 선택
   - "Import" 클릭

3. **프로젝트 설정**
   - Framework Preset: Next.js (자동 감지)
   - Root Directory: `./` (기본값)
   - Build Command: `npm run build` (기본값)

4. **환경 변수 설정** ⭐

   | 키 | 값 | 설명 |
   |---|---|---|
   | `NEXT_PUBLIC_SUPABASE_URL` | `https://your-project.supabase.co` | Supabase URL |
   | `NEXT_PUBLIC_SUPABASE_ANON_KEY` | `eyJhbGc...` | Supabase Anon Key |
   | `SUPABASE_SERVICE_ROLE_KEY` | `eyJhbGc...` | Supabase Service Role Key |
   | `TEACHER_SECRET_CODE` | `your-secret-code` | 교사 가입 코드 |
   | `GOOGLE_API_KEY` | `AIza...` | Google Gemini API Key |
   | `NEXT_PUBLIC_SPRITE_API_URL` | `https://your-service.railway.app` | **Railway URL 입력!** |
   | `NEXT_PUBLIC_SITE_URL` | `https://your-project.vercel.app` | **Vercel 배포 URL (OAuth 리다이렉트용)** |

   **중요**:
   - 모든 환경 변수는 **Production**, **Preview**, **Development** 체크
   - `NEXT_PUBLIC_SPRITE_API_URL`에 위에서 받은 Railway URL 입력
   - `NEXT_PUBLIC_SITE_URL`에 Vercel 배포 URL 입력 (OAuth 로그인 리다이렉트에 사용)

5. **배포 시작**
   - "Deploy" 버튼 클릭
   - 약 2-3분 대기

6. **배포 완료**
   - `https://your-project.vercel.app` URL 제공
   - "Visit" 버튼으로 사이트 확인

**✅ Vercel 배포 완료!**

---

## 3. 도메인 연결 (선택)

### 3.1 Vercel 커스텀 도메인

1. Vercel 대시보드 → 프로젝트 → "Settings" → "Domains"
2. 도메인 입력 (예: `myapp.com`)
3. DNS 설정:
   ```
   Type: A
   Name: @
   Value: 76.76.21.21

   Type: CNAME
   Name: www
   Value: cname.vercel-dns.com
   ```
4. DNS 전파 대기 (1-48시간)

### 3.2 Railway 서브도메인 (선택)

1. Railway → "Settings" → "Networking" → "Add Domain"
2. `api.myapp.com` 입력
3. DNS 설정:
   ```
   Type: CNAME
   Name: api
   Value: your-service.railway.app
   ```
4. Vercel 환경 변수 업데이트:
   - `NEXT_PUBLIC_SPRITE_API_URL`을 `https://api.myapp.com`으로 변경
   - Redeploy

---

## 4. 로컬 개발 환경 (선택)

### 4.1 .env.local 파일 생성

프로젝트 루트에 `.env.local` 파일 생성:

```bash
# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGc...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGc...

# Google Gemini API
GOOGLE_API_KEY=AIza...

# Teacher Secret Code
TEACHER_SECRET_CODE=your-secret-code

# Sprite API (로컬 FastAPI 서버)
NEXT_PUBLIC_SPRITE_API_URL=http://localhost:8000

# Site URL (OAuth 리다이렉트용, 로컬에서는 생략 가능)
# NEXT_PUBLIC_SITE_URL=http://localhost:3000
```

### 4.2 로컬 서버 실행

**Terminal 1 - Next.js:**
```bash
npm run dev
```

**Terminal 2 - Python 백엔드 (선택):**
```bash
cd zepimg_111
python api_server.py
```

---

## 5. 배포 후 테스트

### 5.1 기본 기능 테스트

- [ ] 메인 페이지 로드
- [ ] Google 소셜 로그인
- [ ] 학생 회원가입/로그인
- [ ] 학생 대시보드 (public + targeted 콘텐츠 표시)

### 5.2 교사 기능 테스트

- [ ] 교사 계정 로그인
- [ ] 콘텐츠 생성 (Manual Entry)
- [ ] 콘텐츠 생성 (AI Generator + Gemini TTS)
- [ ] **특정 학생 맞춤형 콘텐츠 생성** ⭐ 새 기능
- [ ] 숙제 할당 (권한 있는 학생만)

### 5.3 데이터베이스 확인

Supabase 대시보드:
- [ ] `study_sets.targeted_students` 컬럼 존재
- [ ] RLS 정책 정상 작동
- [ ] Storage에 오디오/이미지 업로드 확인

### 5.4 Python 백엔드 테스트

```bash
# Health Check
curl https://your-service.railway.app/

# Sprite Maker 페이지에서 이미지 업로드 테스트
```

---

## 6. 문제 해결

### 문제 1: Vercel 빌드 실패

**해결**:
1. Vercel 로그 확인
2. 로컬에서 `npm run build` 실행
3. 환경 변수 모두 설정되었는지 확인

### 문제 2: Railway 배포 실패

**해결**:
1. Railway 로그 확인
2. Root Directory가 `zepimg_111`인지 확인
3. `requirements.txt` 패키지 확인

### 문제 3: Sprite 기능 작동 안 함

**해결**:
1. `NEXT_PUBLIC_SPRITE_API_URL` 환경 변수 확인
2. Railway 서버 실행 중인지 확인
3. CORS 설정 확인 (`api_server.py`)

### 문제 4: Gemini TTS 오류

**해결**:
1. `GOOGLE_API_KEY` 환경 변수 확인
2. Vercel 로그에서 오디오 생성 로그 확인:
   ```
   ✅ Audio found in candidate.content.parts
   - MIME Type: audio/L16;codec=pcm;rate=24000
   - Data size: 156000 characters (base64)
   🔄 Converting PCM to WAV...
   ✅ Conversion complete - WAV size: 156044 characters
   ```

### 문제 5: 학생 대시보드에 할당된 콘텐츠 안 보임

**해결**:
1. `study_sets.targeted_students` 배열에 학생 ID 포함되었는지 확인
2. 학생 대시보드 쿼리 확인:
   ```typescript
   .or(`is_public.eq.true,owner_id.eq.${user.id},targeted_students.cs.{${user.id}}`)
   ```

### 문제 6: 환경 변수 미적용

**해결**:
1. Vercel/Railway 대시보드에서 환경 변수 확인
2. **Redeploy** 클릭 (환경 변수 변경 시 필수)
3. `NEXT_PUBLIC_*` 변수는 빌드 타임에 적용됨

### 문제 7: OAuth 로그인 후 리다이렉트 에러 ⭐ 자주 발생!

**증상**:
- 로그인 버튼 클릭 후 Google 로그인은 되지만 앱으로 돌아오지 못함
- "Invalid redirect URL" 에러 발생
- localhost로 리다이렉트 시도

**해결**:
1. **Supabase 대시보드** 확인
   - Authentication → URL Configuration
2. **Site URL 확인**:
   - Production: `https://your-project.vercel.app`
   - Development: `http://localhost:3000`
3. **Redirect URLs에 추가**:
   ```
   http://localhost:3000/auth/callback
   https://your-project.vercel.app/auth/callback
   ```
4. **Vercel 환경 변수 확인**:
   - `NEXT_PUBLIC_SITE_URL=https://your-project.vercel.app`
5. **저장 후 재배포**

**중요**: Redirect URL은 정확히 일치해야 함 (trailing slash 주의!)

---

## 7. 체크리스트

### 배포 전

- [ ] **Supabase Authentication 설정 완료** ⭐ 중요!
  - [ ] Site URL 설정
  - [ ] Redirect URLs에 localhost + Vercel URL 추가
- [ ] Supabase 마이그레이션 완료 (`targeted_students` 컬럼 추가)
- [ ] 모든 환경 변수 준비
- [ ] 로컬에서 `npm run build` 성공
- [ ] `.gitignore`에 `.env.local` 포함

### 배포 후

- [ ] Railway 배포 성공 및 URL 확보
- [ ] Vercel 배포 성공
- [ ] SSL 인증서 활성화
- [ ] 모든 기능 테스트 완료
- [ ] 팀원과 배포 URL 공유

---

## 자동 배포 (CI/CD)

**이미 활성화됨!**

```bash
git add .
git commit -m "feat: 새 기능 추가"
git push origin main
```

→ Vercel과 Railway가 자동으로 빌드 & 배포 시작

---

## 비용 예상

- **Vercel Hobby**: 무료 (100GB 대역폭/월)
- **Railway Free Trial**: $5 크레딧
- **Supabase Free**: 500MB DB, 1GB Storage
- **총 비용**: $0 (무료 플랜으로 시작 가능)

---

## 유용한 링크

- **Vercel 문서**: https://vercel.com/docs
- **Railway 문서**: https://docs.railway.app
- **Next.js 문서**: https://nextjs.org/docs
- **Supabase 문서**: https://supabase.com/docs
- **Google Gemini API**: https://ai.google.dev

---

배포 과정에서 문제가 생기면 언제든지 문의하세요! 🚀
